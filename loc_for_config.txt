import sys
import os

# Print the current working directory
print("Current Working Directory:", os.getcwd())

# Add the root directory of your project to sys.path
sys.path.append(r'F:\ETL Project 1')  # Adjust this to the correct directory if needed

# Now try importing from etl_config
try:
    from etl_config import api_key
    print(f"API Key: {api_key}")  # Check if api_key is successfully loaded
except ImportError as e:
    print(f"Error importing etl_config: {e}")